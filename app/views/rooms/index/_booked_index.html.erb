<%= render layout: '/layouts/trip' do %>
  <%= render layout: '/layouts/transparent_card' do %>
    <% if @room.photo %>
      <%= image_tag @room.photo, class:"img-fluid room_img" %>
    <% end %>
    <div class="col-12 col-sm-8 text-dark">
      <p class="text-truncate text-center text-sm-left mb-3">
        <%= link_to trip_room_path(@room, trip_id: @room.trip.id) do %>
          <strong><%= @room.name? ? @room.name : "Non identifié" %></strong>
        <% end %>
      </p>
      <div class="d-none d-sm-block">
        <p class="my-3"><small>Votre réservation est confirmée !</small></p>
        <p class="my-3"><%= link_to "Revoir #{"sur #{@room.website}" if !@room.website.nil? }", @room.url, target: :_blank, class: "btn btn-outline-primary", style: "font-size: 0.8rem" %></p>
      </div>
      <div class="d-block d-sm-none text-center mb-3 ">
        <p class="col-12 my-3"><small>Votre réservation est confirmée !</small></p>
        <p class="my-3"><%= link_to "Revoir #{"sur #{@room.website}" if !@room.website.nil? }", @room.url, target: :_blank, class: "btn btn-outline-primary", style: "font-size: 0.8rem" %></p>
      </div>
    </div>
  <% end %>
<% end %>
